# 🚀 خطوات نشر تحديثات الباك إند على Render

## التحديثات الجديدة:
- دعم استقبال معلومات السياق (context) من الفرونت إند
- دمج معلومات السياق مع DALMA_AI_SYSTEM_MESSAGE
- معلومات الوقت، الموقع، الطقس، وأوقات الصلاة

## خطوات النشر:

### 1. افتح Render Dashboard
- اذهب إلى: https://dashboard.render.com/
- سجل الدخول بحسابك

### 2. اختر الخدمة
- اضغط على **dalma-api** (Web Service)

### 3. انتقل إلى Manual Deploy
- من القائمة الجانبية اختر **Manual Deploy**
- أو اضغط على **Deploy** في أعلى الصفحة

### 4. ابدأ النشر
- اضغط على زر **Deploy latest commit**
- أو **Clear build cache & deploy** إذا أردت نشر نظيف

### 5. انتظر اكتمال النشر
- ستظهر لك شاشة البناء (Build logs)
- انتظر حتى تظهر رسالة **Live** باللون الأخضر
- العملية تستغرق عادة 2-5 دقائق

### 6. تأكد من نجاح النشر
- بعد اكتمال النشر، اختبر الـ API:
  ```
  https://dalma-api.onrender.com/
  ```
- يجب أن تظهر رسالة: "مرحباً! API الدلما يعمل بنجاح 🚀"

## ملاحظات مهمة:

### ✅ ما تم تحديثه في الباك إند:
```javascript
// الآن يستقبل context من الفرونت
const { message, history, prompt_id, context } = req.body;

// يدمج معلومات السياق مع التعليمات
if (context) {
  systemMessage += `
  - الوقت: ${context.arabicTime} (${context.timeOfDay})
  - الموقع: ${context.location}
  - الطقس: ${context.weather}
  - أوقات الصلاة...
  `;
}
```

### 🔍 للتحقق من Logs:
1. اذهب إلى **Logs** في Render
2. ابحث عن:
   - `📍 [DALMA AI] Context received:` 
   - `✅ [DALMA AI] تم تحميل التعليمات مع السياق`

### 📱 اختبار من التطبيق:
1. افتح صفحة ذكاء الدلما في التطبيق
2. سترى في رسالة الترحيب:
   - الموقع الحالي
   - الوقت والتاريخ
   - الطقس
   - الصلاة القادمة
3. اسأل عن الصلاة أو الطقس وسيجيب بناءً على معلوماتك الحقيقية

## في حالة وجود مشاكل:
- تحقق من **Logs** في Render
- تأكد من أن GitHub متزامن مع آخر commit
- جرب **Clear build cache & deploy**

---

آخر تحديث: ${new Date().toISOString()}
Commit: إضافة معلومات السياق وأوقات الصلاة لذكاء الدلما
