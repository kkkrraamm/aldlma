# 🎨 تحسينات نظام الإعلانات

## 📋 نظرة عامة

تم تحسين نظام الإعلانات بشكل شامل لتوفير تجربة أفضل للمعلنين والمستخدمين.

---

## ✨ التحسينات المُنفذة

### **1. 📸 مواصفات الصور المفصلة**

#### **في نموذج الإعلان:**
```
✅ الأبعاد الموصى بها لكل صفحة
✅ الحد الأقصى للملف (5MB)
✅ الصيغ المدعومة (JPG, PNG, GIF, WebP)
✅ نسب العرض إلى الارتفاع
```

#### **المواصفات حسب الصفحة:**

| الصفحة | الأبعاد الموصى بها | النسبة | الاستخدام |
|--------|-------------------|--------|-----------|
| **الرئيسية** | 1200×400px | 3:1 | قسم "العروض والإعلانات" |
| **الخدمات** | 800×300px | 8:3 | تحت زر "الكل" وحسب الفئة |
| **العقارات** | 600×400px | 3:2 | ضمن القائمة وصفحة الإضافة |
| **الترندات** | 728×90px | 8:1 | بانر أفقي بعد الصحفيين |

#### **المظهر في النموذج:**
```html
<div class="form-hint">
    <i class="fas fa-info-circle"></i> 
    <strong>الأبعاد الموصى بها:</strong>
    <ul>
        <li><strong>الصفحة الرئيسية:</strong> 1200×400 بكسل (نسبة 3:1)</li>
        <li><strong>صفحة الخدمات:</strong> 800×300 بكسل (نسبة 8:3)</li>
        <li><strong>صفحة العقارات:</strong> 600×400 بكسل (نسبة 3:2)</li>
        <li><strong>صفحة الترندات:</strong> 728×90 بكسل (بانر أفقي)</li>
        <li><strong>الحد الأقصى للملف:</strong> 5 ميجابايت</li>
        <li><strong>الصيغ المدعومة:</strong> JPG, PNG, GIF, WebP</li>
    </ul>
</div>
```

---

### **2. 🗺️ اختيار المسار الداخلي المحسّن**

#### **التنظيم:**
```
📁 الصفحات الرئيسية (مع شريط التنقل)
   🏠 الصفحة الرئيسية
   🛠️ الخدمات
   🏘️ العقارات
   📈 الترندات
   📋 طلباتي

📁 الأدوات والميزات (مع شريط التنقل)
   🕌 أوقات الصلاة
   🤖 ذكاء الدلما
   ✈️ رحلات الطيران

📁 صفحات فرعية (بدون شريط التنقل)
   ➕ إضافة عقار
   👤 الملف الشخصي
```

#### **الملاحظة التوضيحية:**
```
💡 الصفحات في المجموعة الأولى والثانية تحافظ على شريط التنقل السفلي،
   بينما الصفحات الفرعية تفتح بدون شريط تنقل.
```

#### **الفائدة:**
- ✅ المعلن يعرف بالضبط ما سيحدث عند الضغط على الإعلان
- ✅ تجنب الارتباك حول شريط التنقل
- ✅ تنظيم أفضل للمسارات

---

### **3. ⌨️ اختصار لوحة المفاتيح (Shift + R)**

#### **الوظيفة:**
```
اضغط Shift + R → يظهر دليل مواقع الإعلانات
اضغط Shift + R مرة أخرى → يختفي الدليل
```

#### **محتوى الدليل:**

##### **بطاقات ملونة لكل صفحة:**

**🟢 الصفحة الرئيسية (أخضر):**
```
الموقع: home
الموضع: top, middle, bottom
الحجم الموصى به: 1200×400px
الوصف: يظهر في قسم "العروض والإعلانات"
```

**🔵 صفحة الخدمات (أزرق):**
```
الموقع: services
الموضع: top, middle, bottom
الحجم الموصى به: 800×300px
الوصف: يظهر تحت زر "الكل" وحسب الفئة المختارة
```

**🟠 صفحة العقارات (برتقالي):**
```
الموقع: realty
الموضع: top, middle, bottom
الحجم الموصى به: 600×400px
الوصف: يظهر ضمن قائمة العقارات وفي صفحة إضافة عقار
```

**🟣 صفحة الترندات (بنفسجي):**
```
الموقع: trends
الموضع: top, middle, bottom
الحجم الموصى به: 728×90px
الوصف: يظهر بعد قائمة الصحفيين (بانر أفقي)
```

##### **نصائح مهمة:**
```
✅ الترتيب: استخدم رقم الترتيب (display_order) للتحكم في ترتيب ظهور الإعلانات
✅ الموضع: top (أعلى الصفحة)، middle (وسط الصفحة)، bottom (أسفل الصفحة)
✅ التاريخ: حدد تاريخ البداية والنهاية لعرض الإعلان تلقائياً
✅ الحركة: إذا كان هناك أكثر من إعلان في نفس الموقع، سيتم عرضها بشكل متحرك (carousel)
✅ الاختصار: اضغط Shift + R لإظهار/إخفاء هذا الدليل
```

#### **التنفيذ:**
```javascript
// في ads-management.js
document.addEventListener('keydown', (e) => {
    if (e.key === 'r' && e.shiftKey && !e.ctrlKey && !e.altKey && !e.metaKey) {
        e.preventDefault();
        const overlay = document.getElementById('adLocationsOverlay');
        overlay.style.display = overlay.style.display === 'none' ? 'block' : 'none';
    }
});
```

---

### **4. 🎠 Carousel تلقائي للإعلانات المتعددة**

#### **الميزات:**
```
✅ عرض متحرك تلقائي كل 5 ثوانٍ
✅ انتقال سلس بين الإعلانات
✅ مؤشرات (نقاط) تظهر الإعلان الحالي
✅ إمكانية التمرير اليدوي
✅ إيقاف تلقائي عند الخروج من الصفحة
```

#### **كيف يعمل:**

##### **1. إذا كان هناك إعلان واحد:**
```
📸 [إعلان واحد]
   - يظهر بشكل ثابت
   - لا توجد مؤشرات
   - لا حركة تلقائية
```

##### **2. إذا كان هناك أكثر من إعلان:**
```
📸 [إعلان 1] ● ○ ○
   ↓ (بعد 5 ثوانٍ)
📸 [إعلان 2] ○ ● ○
   ↓ (بعد 5 ثوانٍ)
📸 [إعلان 3] ○ ○ ●
   ↓ (يعود للأول)
📸 [إعلان 1] ● ○ ○
```

#### **التنفيذ (Flutter):**
```dart
// Auto-play timer
Timer? _autoPlayTimer;
PageController _pageController;

void _startAutoPlay() {
  if (_ads.length <= 1) return;
  
  _autoPlayTimer = Timer.periodic(const Duration(seconds: 5), (timer) {
    if (_pageController.hasClients) {
      final nextPage = (_currentPage + 1) % _ads.length;
      _pageController.animateToPage(
        nextPage,
        duration: const Duration(milliseconds: 500),
        curve: Curves.easeInOut,
      );
    }
  });
}

// Page indicators
if (_ads.length > 1)
  Row(
    mainAxisAlignment: MainAxisAlignment.center,
    children: List.generate(
      _ads.length,
      (index) => Container(
        width: 8,
        height: 8,
        decoration: BoxDecoration(
          shape: BoxShape.circle,
          color: _currentPage == index
              ? Color(0xFF10b981)  // أخضر للنشط
              : Colors.grey.withOpacity(0.4),  // رمادي للباقي
        ),
      ),
    ),
  ),
```

---

### **5. 📊 نظام الترتيب (Display Order)**

#### **كيف يعمل:**
```sql
-- في Backend
SELECT * FROM ads 
WHERE page_location = $1 
  AND is_active = true
ORDER BY display_order ASC, created_at DESC
```

#### **الأولوية:**
```
1. display_order (الأقل أولاً)
2. created_at (الأحدث إذا تساوى الترتيب)
```

#### **مثال:**
```
إعلان A: display_order = 1 → يظهر أولاً
إعلان B: display_order = 2 → يظهر ثانياً
إعلان C: display_order = 3 → يظهر ثالثاً
إعلان D: display_order = 1 → يظهر مع A (حسب التاريخ)
```

#### **في Carousel:**
```
إذا كان هناك 3 إعلانات في نفس الموقع والموضع:
1. يتم ترتيبها حسب display_order
2. تظهر في carousel بنفس الترتيب
3. تتحرك تلقائياً كل 5 ثوانٍ
```

---

## 🎯 تجربة المستخدم

### **للمعلن (Admin Pro):**

#### **1. عند إضافة إعلان:**
```
✅ يرى المواصفات المطلوبة بوضوح
✅ يعرف الحجم الموصى به لكل صفحة
✅ يختار المسار الداخلي بسهولة
✅ يعرف إذا كان شريط التنقل سيظهر أم لا
✅ يمكنه الضغط Shift + R لرؤية جميع المواقع
```

#### **2. عند تحديد الترتيب:**
```
✅ رقم أقل = أولوية أعلى
✅ يظهر في Carousel بنفس الترتيب
✅ واضح ومباشر
```

### **للمستخدم (التطبيق):**

#### **1. عند رؤية الإعلانات:**
```
✅ إعلان واحد: ثابت وواضح
✅ عدة إعلانات: carousel متحرك
✅ مؤشرات تظهر الموقع الحالي
✅ يمكن التمرير يدوياً
✅ حركة سلسة وجميلة
```

#### **2. عند الضغط على الإعلان:**
```
✅ روابط خارجية: تفتح في المتصفح
✅ مسارات داخلية: تنقل داخل التطبيق
✅ شريط التنقل: يظهر في الصفحات الرئيسية فقط
✅ تتبع النقرات: يتم تسجيلها تلقائياً
```

---

## 📊 الإحصائيات والتتبع

### **ما يتم تتبعه:**
```
✅ المشاهدات (Impressions): عند تحميل الإعلان
✅ النقرات (Clicks): عند الضغط على الإعلان
✅ معدل النقر (CTR): النقرات ÷ المشاهدات × 100
```

### **في Carousel:**
```
✅ كل إعلان يُحسب مشاهدة عند ظهوره
✅ التمرير اليدوي يُحسب مشاهدة جديدة
✅ الحركة التلقائية تُحسب مشاهدة جديدة
```

---

## 🔧 التفاصيل التقنية

### **Frontend (Admin Pro):**
```javascript
// Keyboard shortcut
document.addEventListener('keydown', (e) => {
    if (e.key === 'r' && e.shiftKey) {
        // Toggle ad locations guide
    }
});
```

### **Frontend (Flutter App):**
```dart
// Auto-play carousel
Timer.periodic(Duration(seconds: 5), (timer) {
    _pageController.animateToPage(nextPage);
});

// Page indicators
List.generate(_ads.length, (index) => Dot(
    active: _currentPage == index
));
```

### **Backend:**
```javascript
// Fetch ads with ordering
SELECT * FROM ads 
WHERE page_location = $1 
  AND is_active = true
  AND (start_date IS NULL OR start_date <= NOW())
  AND (end_date IS NULL OR end_date >= NOW())
ORDER BY display_order ASC, created_at DESC
```

---

## ✅ الخلاصة

### **ما تم تحسينه:**
```
✅ مواصفات الصور واضحة ومفصلة
✅ اختيار المسار الداخلي منظم ومفهوم
✅ دليل مواقع الإعلانات (Shift + R)
✅ Carousel تلقائي للإعلانات المتعددة
✅ نظام الترتيب يعمل بشكل صحيح
✅ تجربة مستخدم محسّنة
✅ تتبع دقيق للإحصائيات
```

### **النتيجة:**
```
🎯 المعلن يعرف بالضبط ما يفعل
🎨 الإعلانات تظهر بشكل احترافي
📊 الإحصائيات دقيقة وموثوقة
✨ تجربة مستخدم ممتازة
```

---

**🎉 نظام الإعلانات الآن احترافي ومتكامل! ✨**

