# 🔍 أدوات تشخيص وتحليل الأخطاء - Dalma App

## 📋 المحتويات

تم إنشاء أداتين قويتين لمساعدتك في تشخيص وتحليل أخطاء التطبيق:

### 1️⃣ `debug_monitor.sh` - المراقب الحي للوقز
مدقق تلقائي يعمل في الوقت الفعلي أثناء تشغيل التطبيق

### 2️⃣ `analyze_logs.sh` - محلل اللوقز
يحلل لوقز موجودة بالفعل (من Terminal أو ملفات)

---

## 🚀 الاستخدام

### الطريقة 1️⃣: المراقبة الحية (أثناء التشغيل)

```bash
cd /Users/kimaalanzi/Desktop/aaldma/aldlma
./debug_monitor.sh
```

**ماذا يحدث؟**
- ✅ يشغل التطبيق تلقائياً
- 🔍 يراقب جميع اللوقز في الوقت الفعلي
- ⚠️ يكتشف الأخطاء تلقائياً ويصنفها
- 📊 عند الضغط على `Ctrl+C` يعرض تقرير شامل

**مثال على الناتج:**
```
🔍 مدقق اللوقز التلقائي - Dalma App
════════════════════════════════════════════════════

⏳ جارٍ مراقبة اللوقز...
   اضغط Ctrl+C للإيقاف وعرض التقرير

❌ [TYPE ERROR] type 'String' is not a subtype of type 'int' of 'index'
⚠️  [NETWORK ERROR] Connection timeout
✅ [SUCCESS] User logged in successfully

[Ctrl+C للتقرير]

════════════════════════════════════════════════════
           📊 تقرير تحليل الأخطاء
════════════════════════════════════════════════════

📈 إجمالي الأخطاء: 2

┌─ تفصيل الأخطاء حسب النوع:
├─ Type Errors: 1 ❌
└─ Network Errors: 1 ⚠️

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔴 Type Errors (أخطاء الأنواع - الأكثر أهمية):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   • [10:05:23] type 'String' is not a subtype...

💡 الحل المقترح:
   - تحقق من استخدام cast<T>() واستبدله بـ map().toList()
   - تأكد من تحويل الأنواع بشكل صريح
```

---

### الطريقة 2️⃣: تحليل لوقز موجودة

```bash
cd /Users/kimaalanzi/Desktop/aaldma/aldlma
./analyze_logs.sh
```

**كيف تستخدمه؟**

#### Option A: نسخ ولصق من Terminal
```bash
./analyze_logs.sh
# سيطلب منك لصق اللوقز
[قم بلصق اللوقز هنا]
# اضغط Ctrl+D للانتهاء
```

#### Option B: من ملف
```bash
cat flutter_logs.txt | ./analyze_logs.sh
```

#### Option C: من الحافظة (Clipboard)
```bash
pbpaste | ./analyze_logs.sh
```

**مثال عملي:**
```bash
# 1. شغّل التطبيق عادياً
flutter run > logs.txt 2>&1

# 2. عند ظهور خطأ، أوقف التطبيق (Ctrl+C)

# 3. حلل اللوقز
cat logs.txt | ./analyze_logs.sh
```

---

## 📊 أنواع الأخطاء التي يكتشفها

| النوع | الرمز | الخطورة | أمثلة |
|------|------|---------|-------|
| **Type Errors** | ❌ | 🔴 حرجة | `type 'String' is not a subtype`, `cast failed` |
| **Null Errors** | ❌ | 🔴 حرجة | `null check operator`, `null exception` |
| **State Errors** | ❌ | 🔴 حرجة | `setState called after dispose`, `bad state` |
| **Network Errors** | ⚠️ | 🟡 متوسطة | `404`, `timeout`, `connection refused` |
| **Database Errors** | ❌ | 🔴 حرجة | `SQL error`, `relation does not exist` |
| **Render Errors** | ⚠️ | 🟡 متوسطة | `overflow by X pixels`, `renderbox not laid out` |

---

## 🎯 حلول الأخطاء الشائعة

### ❌ Type Error: `'String' is not a subtype of 'int' of 'index'`

**السبب:**
```dart
// ❌ استخدام cast<String>() خطأ شائع
List<String> items = data.cast<String>();
```

**الحل:**
```dart
// ✅ استخدم map().toList()
List<String> items = data.map((e) => e.toString()).toList();
```

**الملفات المتأثرة:**
- `lib/trends_page.dart`
- `lib/media_posts_page.dart`
- أي ملف يستخدم `cast<T>()`

---

### ❌ Null Error

**السبب:**
```dart
// ❌ عدم التحقق من null
String name = user['name'];
```

**الحل:**
```dart
// ✅ استخدام null safety
String name = user['name'] ?? 'غير معروف';
String? email = user['email'];
int age = int.tryParse(user['age']) ?? 0;
```

---

### ❌ setState after dispose

**السبب:**
```dart
// ❌ استدعاء setState بعد dispose
Future<void> loadData() async {
  final data = await fetchData();
  setState(() => _data = data); // قد يحدث بعد dispose!
}
```

**الحل:**
```dart
// ✅ التحقق من mounted
Future<void> loadData() async {
  final data = await fetchData();
  if (mounted) {
    setState(() => _data = data);
  }
}
```

---

## 🛠️ نصائح للاستخدام الفعال

### 1. استخدم المراقب الحي أثناء التطوير
```bash
# في Terminal منفصل، شغّل المراقب
./debug_monitor.sh

# اختبر التطبيق واضغط Ctrl+C عند الانتهاء
```

### 2. احفظ اللوقز للمراجعة لاحقاً
```bash
flutter run > logs_$(date +%Y%m%d_%H%M%S).txt 2>&1
```

### 3. قارن اللوقز قبل وبعد الإصلاح
```bash
# قبل
flutter run > logs_before.txt 2>&1
# [اختبار]
# [Ctrl+C]

# بعد الإصلاح
flutter run > logs_after.txt 2>&1
# [اختبار]
# [Ctrl+C]

# مقارنة
diff logs_before.txt logs_after.txt
```

---

## 📝 أمثلة عملية

### مثال 1: اكتشاف خطأ Type مباشرة

```bash
$ ./debug_monitor.sh

flutter: ✅ Loading data...
flutter: ❌ type 'String' is not a subtype of type 'int' of 'index'
^C

📊 تقرير تحليل الأخطاء
═══════════════════════════════
Type Errors: 1 ❌

🔴 Type Errors:
   • [10:30:45] type 'String' is not a subtype...

💡 الحل المقترح:
   - استخدم map().toList() بدلاً من cast<String>()
```

### مثال 2: تحليل لوقز من ملف

```bash
$ cat old_logs.txt | ./analyze_logs.sh

🔎 جارٍ تحليل اللوقز...

❌ [TYPE] type 'String' is not a subtype...
❌ [NULL] null check operator used on null value
⚠️  [NETWORK] 404 Not Found

📊 نتائج التحليل
═══════════════════════════════
📈 إجمالي الأخطاء: 3

├─ Type Errors: 1 ❌
├─ Null Errors: 1 ❌
└─ Network Issues: 1 ⚠️
```

---

## 🔧 التخصيص

يمكنك تعديل السكريبتات حسب احتياجاتك:

### إضافة نوع خطأ جديد

افتح `debug_monitor.sh` أو `analyze_logs.sh` وأضف:

```bash
# بعد السطر الذي يحتوي على "فحص أخطاء Type"
elif echo "$line" | grep -iq "YOUR_ERROR_PATTERN"; then
    ((YOUR_ERROR_COUNT++))
    YOUR_ERROR_DETAILS+=("[$timestamp] $line")
    echo -e "${RED}❌ [YOUR_ERROR] $line${NC}"
```

---

## 📞 الدعم

إذا واجهت مشكلة في استخدام الأدوات:

1. تأكد من الصلاحيات: `chmod +x *.sh`
2. تأكد من تثبيت Flutter: `flutter --version`
3. تأكد من وجودك في المجلد الصحيح: `pwd`

---

## ✅ الخلاصة

| الأداة | متى تستخدمها | الفائدة |
|-------|-------------|---------|
| `debug_monitor.sh` | أثناء التطوير والاختبار | اكتشاف فوري للأخطاء |
| `analyze_logs.sh` | بعد حدوث خطأ | تحليل تفصيلي وحلول مقترحة |

**🎉 الآن أصبح لديك نظام تشخيص احترافي للتطبيق!**

