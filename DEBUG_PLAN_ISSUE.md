# 🔍 تشخيص مشكلة عرض الباقة "مجاني"

## 📋 **الخطوات:**

### 1️⃣ **أعد تحميل الصفحة:**
```
Ctrl + Shift + R (إعادة تحميل كاملة)
```

### 2️⃣ **افتح Console:**
```
F12 → Console
```

### 3️⃣ **اضغط على "إداري":**
```
ابحث عن هذه الرسائل:
✅ [REFRESH] تم تحديث بيانات المكتب: { code: 'pro', name_ar: 'احترافي', ... }
📊 [USER MENU] Office Data: { ... }
📊 [USER MENU] Plan: { code: 'pro', name_ar: 'احترافي', ... }
📊 [USER MENU] Current User: { plan: 'احترافي', planCode: 'pro', ... }
```

---

## 🔍 **التحقق من البيانات:**

### في Console، اكتب:
```javascript
// 1. التوكن
localStorage.getItem('office_token')

// 2. بيانات المكتب
JSON.parse(localStorage.getItem('office_data'))

// 3. الباقة
JSON.parse(localStorage.getItem('office_data')).plan
```

---

## 🎯 **السيناريوهات المحتملة:**

### السيناريو 1: API لا يرجع البيانات الصحيحة
```
❌ المشكلة: /api/office/info يرجع plan_code = 'free'
✅ الحل: تحقق من قاعدة البيانات
```

### السيناريو 2: localStorage قديم
```
❌ المشكلة: البيانات المخزنة قديمة
✅ الحل: امسح localStorage وأعد تسجيل الدخول
```

### السيناريو 3: التوكن خاطئ
```
❌ المشكلة: التوكن لا يطابق المكتب الصحيح
✅ الحل: أعد تسجيل الدخول
```

---

## 🛠️ **الحلول السريعة:**

### الحل 1: مسح Cache
```javascript
// في Console
localStorage.clear();
// ثم أعد تسجيل الدخول
```

### الحل 2: تحديث يدوي
```javascript
// في Console
await window.refreshOfficeData();
console.log(JSON.parse(localStorage.getItem('office_data')).plan);
```

### الحل 3: تحقق من قاعدة البيانات
```sql
SELECT 
    o.id,
    o.name,
    s.plan_id,
    p.code,
    p.name_ar
FROM realty_offices o
LEFT JOIN realty_subscriptions s ON o.id = s.office_id
LEFT JOIN realty_plans p ON s.plan_id = p.id
WHERE o.id = 3;
```

---

## 📤 **أرسل لي:**

```
1. نتيجة Console بعد الضغط على "إداري"
2. نتيجة: JSON.parse(localStorage.getItem('office_data')).plan
3. لقطة شاشة من القائمة
```

---

**سأنتظر النتائج لتحديد المشكلة بدقة!** 🔍

