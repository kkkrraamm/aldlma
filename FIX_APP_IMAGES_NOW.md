# 🚨 حل سريع: الصور لا تظهر في التطبيق

## ❌ المشكلة

```
Failed host lookup: 'via.placeholder.com'
```

**السبب:** بيانات تجريبية قديمة في قاعدة البيانات

---

## ✅ الحل (3 دقائق)

### **الخطوة 1: افتح Render Dashboard**

1. اذهب إلى: https://dashboard.render.com
2. سجل دخول
3. اختر قاعدة البيانات الخاصة بك

---

### **الخطوة 2: افتح Shell**

في صفحة قاعدة البيانات:
1. اضغط على "Connect" أو "Shell"
2. سيفتح لك terminal

---

### **الخطوة 3: نفذ هذا الأمر**

انسخ والصق:

```sql
DELETE FROM realty_listing_images WHERE url LIKE '%via.placeholder%';
```

اضغط Enter ✅

---

### **الخطوة 4: أعد تشغيل التطبيق**

في Flutter terminal:

```bash
r
```

(Hot Restart)

---

## 🎉 النتيجة

- ✅ لا مزيد من الأخطاء
- ✅ الصور الحقيقية تظهر
- ✅ العقارات بدون صور تظهر بتصميم افتراضي جميل

---

## 📱 اختبار

1. افتح صفحة العقارات في التطبيق
2. اضغط على أي علامة في الخريطة
3. يجب أن تظهر الصورة (أو التصميم الافتراضي)
4. لا توجد أخطاء في Console ✅

---

## 🔄 البديل: إذا لم يكن لديك وصول لـ Render

**استخدم pgAdmin:**

1. افتح pgAdmin
2. اتصل بقاعدة البيانات
3. افتح Query Tool
4. نفذ نفس الأمر:
   ```sql
   DELETE FROM realty_listing_images WHERE url LIKE '%via.placeholder%';
   ```

---

## 📝 ملاحظة

**العقارات الجديدة التي تضيفها من بوابة المكاتب:**
- ✅ ستحتوي على صور حقيقية من imgbb
- ✅ لن تواجه هذه المشكلة مرة أخرى

**العقارات القديمة:**
- إذا كانت مهمة: أضف لها صور جديدة من بوابة المكاتب
- إذا كانت تجريبية: احذفها

---

## 🎯 الخلاصة

**1 أمر SQL واحد = حل المشكلة!**

```sql
DELETE FROM realty_listing_images WHERE url LIKE '%via.placeholder%';
```

**ثم:**
```bash
r  # في Flutter
```

**✅ تم!**

---

**الوقت المتوقع: 3 دقائق ⏱️**

