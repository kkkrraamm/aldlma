# 🎯 ميزة البحث بالإحداثيات

## ✅ ما تم إضافته:

### ميزتان جديدتان لتحديد الموقع:

1. ✅ **البحث بالإحداثيات** (خط العرض وخط الطول)
2. ✅ **موقعي الحالي** (GPS)

---

## 🎨 الواجهة الجديدة:

```
┌─────────────────────────────────────────────────────────┐
│ الموقع على الخريطة                                     │
├─────────────────────────────────────────────────────────┤
│ ┌──────────────┬──────────────┬────────┬──────────┐   │
│ │ خط العرض     │ خط الطول    │ [بحث] │ [موقعي] │   │
│ │ 30.9753      │ 41.0381      │        │          │   │
│ └──────────────┴──────────────┴────────┴──────────┘   │
│                                                         │
│ [الخريطة]                                              │
│                                                         │
│ 💡 اضغط على الخريطة أو استخدم البحث بالإحداثيات       │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 الميزات:

### 1️⃣ البحث بالإحداثيات:

**كيف تستخدمها:**
1. أدخل خط العرض (مثال: 30.9753)
2. أدخل خط الطول (مثال: 41.0381)
3. اضغط "بحث"

**ما يحدث:**
- ✅ التحقق من صحة الإحداثيات
- ✅ التحقق من أنها داخل المملكة
- ✅ الانتقال للموقع على الخريطة
- ✅ وضع marker تلقائياً
- ✅ حفظ الإحداثيات في الحقول المخفية

**التحقق من الصحة:**
```javascript
// النطاق المسموح (المملكة العربية السعودية)
lat: 15 - 35
lng: 30 - 60
```

---

### 2️⃣ موقعي الحالي (GPS):

**كيف تستخدمها:**
1. اضغط زر "موقعي" 🎯
2. اسمح للمتصفح بالوصول للموقع
3. انتظر ثوانٍ

**ما يحدث:**
- ✅ طلب إذن الوصول للموقع
- ✅ تحديد الموقع الحالي بدقة عالية
- ✅ ملء حقول الإحداثيات تلقائياً
- ✅ الانتقال للموقع على الخريطة
- ✅ وضع marker أزرق مميز 📍

**المميزات:**
- `enableHighAccuracy: true` - دقة عالية
- `timeout: 10000` - مهلة 10 ثوانٍ
- رسائل خطأ واضحة (رفض الإذن، الموقع غير متوفر، إلخ)

---

## 🗺️ أنواع الـ Markers:

### 🟢 Marker عادي (عند البحث بالإحداثيات):
```
لون: أخضر
أيقونة: 🏠
```

### 🔵 Marker موقعي الحالي (عند استخدام GPS):
```
لون: أزرق
أيقونة: 🚶
مميز: له animation pulse
```

---

## 📋 حالات الاستخدام:

### 1️⃣ لديك إحداثيات الأرض من Google Maps:
```
1. انسخ الإحداثيات من Google Maps
2. الصق في الحقول
3. اضغط "بحث"
4. ✅ يظهر الموقع بدقة
```

### 2️⃣ أنت في موقع الأرض حالياً:
```
1. اضغط "موقعي"
2. اسمح بالوصول
3. ✅ يتم تحديد موقعك تلقائياً
```

### 3️⃣ تريد تحديد يدوي على الخريطة:
```
1. اضغط على الخريطة مباشرة
2. ✅ يتم وضع marker
```

---

## 🔧 التفاصيل التقنية:

### التحقق من الإحداثيات:
```javascript
// التحقق من أنها أرقام
if (isNaN(lat) || isNaN(lng)) {
    alert('⚠️ يرجى إدخال إحداثيات صحيحة');
    return;
}

// التحقق من النطاق (السعودية)
if (lat < 15 || lat > 35 || lng < 30 || lng < 60) {
    alert('⚠️ الإحداثيات خارج نطاق المملكة');
    return;
}
```

### استخدام GPS:
```javascript
navigator.geolocation.getCurrentPosition(
    success callback,
    error callback,
    {
        enableHighAccuracy: true,  // دقة عالية
        timeout: 10000,            // 10 ثوانٍ
        maximumAge: 0              // لا cache
    }
);
```

---

## 📂 الملفات المُعدلة:

1. ✅ `dalma-office-portal/add-listing.html`
   - إضافة حقول البحث
   - إضافة زر "موقعي"

2. ✅ `dalma-office-portal/js/add-listing.js`
   - دالة `searchByCoordinates()`
   - دالة `getCurrentLocation()`

3. ✅ `dalma-office-portal/edit-listing.html`
   - نفس التحديثات

4. ✅ `dalma-office-portal/js/edit-listing.js`
   - نفس الدوال
   - ملء تلقائي عند التعديل

---

## 🎯 الفوائد:

### للمستخدم:
- ✅ **أسرع** - لا حاجة للبحث يدوياً
- ✅ **أدق** - إحداثيات دقيقة
- ✅ **أسهل** - 3 طرق للتحديد

### للنظام:
- ✅ دقة عالية في المواقع
- ✅ تجربة مستخدم أفضل
- ✅ خيارات متعددة

---

## 🧪 الاختبار:

### 1️⃣ اختبر البحث بالإحداثيات:
```
افتح "إضافة عقار"
→ أدخل: 30.9753, 41.0381
→ اضغط "بحث"
→ ✅ يجب أن تنتقل الخريطة للموقع
→ ✅ marker يظهر
```

### 2️⃣ اختبر موقعي الحالي:
```
اضغط "موقعي"
→ اسمح بالوصول
→ ✅ يظهر "جاري التحديد..."
→ ✅ يتم تحديد موقعك
→ ✅ marker أزرق يظهر
```

### 3️⃣ اختبر في التعديل:
```
افتح "تعديل عقار"
→ ✅ الإحداثيات محملة في الحقول
→ يمكن تعديلها والبحث من جديد
```

---

## 💡 نصائح:

### للحصول على إحداثيات من Google Maps:
1. افتح Google Maps
2. اضغط بزر الماوس الأيمن على الموقع
3. انسخ الإحداثيات
4. الصق في الحقول

### للحصول على إحداثيات دقيقة:
- استخدم "موقعي" وأنت في الموقع الفعلي
- أو استخدم GPS محترف

---

**الميزة جاهزة للاستخدام الآن!** 🎉

**افتح صفحة إضافة عقار وجرب!** 🗺️✨

