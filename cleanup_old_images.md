# 🧹 تنظيف الصور القديمة من قاعدة البيانات

## 🔍 المشكلة

الخطأ الذي يظهر في التطبيق:
```
Failed host lookup: 'via.placeholder.com'
```

**السبب:** هناك عقارات قديمة في قاعدة البيانات تحتوي على روابط صور من `via.placeholder.com` (بيانات تجريبية قديمة).

---

## ✅ الحل السريع

### **الخيار 1: حذف الصور القديمة من via.placeholder**

افتح **pgAdmin** أو **Render Dashboard** ونفذ هذا الأمر:

```sql
-- حذف جميع الصور التي تحتوي على via.placeholder
DELETE FROM realty_listing_images 
WHERE url LIKE '%via.placeholder%';
```

**النتيجة:** سيتم حذف جميع الصور التجريبية القديمة، والعقارات التي لا تحتوي على صور حقيقية ستظهر بدون صور (مع التصميم الافتراضي).

---

### **الخيار 2: حذف العقارات التجريبية بالكامل**

إذا كانت العقارات القديمة كلها تجريبية ولا تحتاج إليها:

```sql
-- حذف جميع العقارات التي صورها من via.placeholder
DELETE FROM realty_listings 
WHERE id IN (
  SELECT DISTINCT listing_id 
  FROM realty_listing_images 
  WHERE url LIKE '%via.placeholder%'
);
```

**النتيجة:** سيتم حذف العقارات التجريبية بالكامل مع صورها.

---

### **الخيار 3: تحديث الصور القديمة (إذا كنت تريد الاحتفاظ بالعقارات)**

إذا كنت تريد الاحتفاظ بالعقارات ولكن حذف الصور فقط:

```sql
-- حذف الصور القديمة فقط
DELETE FROM realty_listing_images 
WHERE url LIKE '%via.placeholder%';

-- الآن يمكنك رفع صور جديدة من بوابة المكاتب
```

---

## 🔍 التحقق من البيانات

### **1. عرض جميع العقارات مع عدد الصور:**

```sql
SELECT 
  l.id,
  l.title,
  l.city,
  l.office_id,
  COUNT(li.id) as image_count,
  STRING_AGG(SUBSTRING(li.url, 1, 60), E'\n') as sample_urls
FROM realty_listings l
LEFT JOIN realty_listing_images li ON l.id = li.listing_id
GROUP BY l.id, l.title, l.city, l.office_id
ORDER BY l.id DESC;
```

### **2. عرض الصور التي تحتوي على via.placeholder:**

```sql
SELECT 
  li.id,
  li.listing_id,
  l.title,
  li.url,
  li.sort_order
FROM realty_listing_images li
JOIN realty_listings l ON li.listing_id = l.id
WHERE li.url LIKE '%via.placeholder%'
ORDER BY li.listing_id, li.sort_order;
```

### **3. عرض العقارات التي لا تحتوي على صور:**

```sql
SELECT 
  l.id,
  l.title,
  l.city,
  l.office_id,
  l.created_at
FROM realty_listings l
LEFT JOIN realty_listing_images li ON l.id = li.listing_id
WHERE li.id IS NULL
ORDER BY l.id DESC;
```

---

## 🚀 الخطوات الموصى بها

### **الطريقة الأفضل (خطوة بخطوة):**

1. **افتح Render Dashboard:**
   - اذهب إلى: https://dashboard.render.com
   - افتح قاعدة البيانات الخاصة بك

2. **افتح Shell:**
   - اضغط على "Shell" أو "Connect"
   - سيفتح لك terminal

3. **نفذ الأمر التالي:**
   ```sql
   -- حذف الصور التجريبية فقط
   DELETE FROM realty_listing_images 
   WHERE url LIKE '%via.placeholder%';
   ```

4. **تحقق من النتيجة:**
   ```sql
   -- عرض العقارات مع عدد الصور
   SELECT 
     l.id,
     l.title,
     COUNT(li.id) as image_count
   FROM realty_listings l
   LEFT JOIN realty_listing_images li ON l.id = li.listing_id
   GROUP BY l.id, l.title
   ORDER BY l.id DESC
   LIMIT 20;
   ```

5. **أعد تشغيل التطبيق:**
   - Hot Restart في Flutter
   - الخطأ يجب أن يختفي

---

## 🎯 النتيجة المتوقعة

### **قبل التنظيف:**
```
❌ Failed host lookup: 'via.placeholder.com'
```

### **بعد التنظيف:**
```
✅ [REALTY] تم تحميل 13 عقار
✅ الصور الحقيقية تظهر
✅ العقارات بدون صور تظهر بالتصميم الافتراضي
✅ لا توجد أخطاء
```

---

## 📱 اختبار التطبيق

بعد تنفيذ التنظيف:

1. **أعد تشغيل التطبيق:**
   ```bash
   r  # Hot Restart
   ```

2. **اذهب إلى صفحة العقارات**

3. **اضغط على أي علامة في الخريطة**

4. **النتيجة المتوقعة:**
   - ✅ إذا كان العقار يحتوي على صور حقيقية → تظهر الصورة الأولى
   - ✅ إذا لم يكن يحتوي على صور → يظهر التصميم الافتراضي (أيقونة المنزل)
   - ✅ لا توجد أخطاء في Console

---

## 🔧 البديل: تحديث الصور من بوابة المكاتب

إذا كنت تريد الاحتفاظ بالعقارات القديمة:

1. **سجل دخول إلى بوابة المكاتب**
2. **اذهب إلى "إدارة العقارات"**
3. **اضغط "تعديل" على أي عقار**
4. **في قسم "إدارة الصور":**
   - احذف الصور القديمة (إن وجدت)
   - ارفع صور جديدة حقيقية
   - اضغط "حفظ التعديلات"
5. **كرر العملية لجميع العقارات القديمة**

---

## 📊 إحصائيات

### **للتحقق من عدد الصور التي سيتم حذفها:**

```sql
-- عدد الصور التجريبية
SELECT COUNT(*) as placeholder_images_count
FROM realty_listing_images
WHERE url LIKE '%via.placeholder%';

-- عدد العقارات المتأثرة
SELECT COUNT(DISTINCT listing_id) as affected_listings_count
FROM realty_listing_images
WHERE url LIKE '%via.placeholder%';
```

---

## ⚠️ ملاحظات مهمة

1. **Backup:** إذا كنت قلقاً، يمكنك أخذ backup قبل الحذف:
   ```sql
   -- إنشاء جدول backup
   CREATE TABLE realty_listing_images_backup AS 
   SELECT * FROM realty_listing_images;
   ```

2. **التراجع:** إذا حذفت بالخطأ:
   ```sql
   -- استعادة من backup
   INSERT INTO realty_listing_images 
   SELECT * FROM realty_listing_images_backup;
   ```

3. **العقارات الجديدة:** جميع العقارات التي تضيفها من الآن فصاعداً ستحتوي على صور حقيقية من imgbb.

---

## ✅ الخلاصة

**المشكلة:** بيانات تجريبية قديمة تحتوي على روابط `via.placeholder.com`

**الحل الأسرع:**
```sql
DELETE FROM realty_listing_images 
WHERE url LIKE '%via.placeholder%';
```

**النتيجة:** 
- ✅ لا مزيد من الأخطاء في التطبيق
- ✅ الصور الحقيقية تظهر بشكل صحيح
- ✅ العقارات بدون صور تظهر بالتصميم الافتراضي

---

**🚀 بعد التنظيف، النظام سيعمل بشكل مثالي!**


