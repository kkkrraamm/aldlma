# 📋 هيكل القائمة الجانبية الموحدة - Dalma Admin Pro

## 🎯 نظرة عامة

القائمة الجانبية موحدة وثابتة في جميع صفحات لوحة التحكم، مع تحديث تلقائي للصفحة النشطة وعدادات الطلبات.

---

## 📊 الهيكل الكامل

### 1. **لوحة التحكم** 📊
```
الرئيسية
├── لوحة التحكم (index.html)
```

---

### 2. **الأمن السيبراني** 🔒
```
الأمن السيبراني
├── إدارة IPs (ip-management.html)
├── مراقبة الأمان (security-monitoring.html)
└── الأدوار والصلاحيات (roles-management.html)
```

**الأيقونات:**
- `fa-network-wired` - إدارة IPs
- `fa-shield-virus` - مراقبة الأمان
- `fa-user-shield` - الأدوار والصلاحيات

---

### 3. **المستخدمين** 👥
```
المستخدمين
└── جميع المستخدمين (users-management.html)
```

**الأيقونات:**
- `fa-users` - جميع المستخدمين

---

### 4. **الطلبات** 📋
```
الطلبات
└── إدارة الطلبات (requests-management.html) [Badge: عدد الطلبات المعلقة]
```

**الأيقونات:**
- `fa-file-alt` - إدارة الطلبات

**Badge:**
- يعرض عدد الطلبات المعلقة
- يتحدث تلقائياً كل 30 ثانية
- يختفي إذا كان العدد = 0

---

### 5. **نظام العقار** 🏢
```
نظام العقار
├── طلبات المكاتب العقارية (office-registrations.html) [Badge]
├── إدارة المكاتب النشطة (offices-management.html)
├── طلبات الترقية (upgrade-requests.html) [Badge]
└── إدارة الاشتراكات المالية (realty-subscriptions.html)
```

**الأيقونات:**
- `fa-building` - طلبات المكاتب العقارية
- `fa-building` - إدارة المكاتب النشطة
- `fa-arrow-up` - طلبات الترقية
- `fa-chart-line` - إدارة الاشتراكات المالية

**Badges:**
- طلبات المكاتب: عدد الطلبات المعلقة
- طلبات الترقية: عدد طلبات الترقية المعلقة

---

### 6. **المالية** 💰
```
المالية
└── الرقابة المالية (finance-monitoring.html)
```

**الأيقونات:**
- `fa-dollar-sign` - الرقابة المالية

---

### 7. **المحتوى والتسويق** 📢
```
المحتوى والتسويق
├── إدارة الإعلانات (ads-management.html) ⭐ NEW
├── إدارة المحتوى (content-management.html)
└── الإشعارات (notifications.html)
```

**الأيقونات:**
- `fa-ad` - إدارة الإعلانات ⭐
- `fa-bullhorn` - إدارة المحتوى
- `fa-bell` - الإشعارات

---

### 8. **التحليلات** 📈
```
التحليلات
├── التقارير (reports.html)
└── التحليلات الذكية (ai-analytics.html)
```

**الأيقونات:**
- `fa-chart-bar` - التقارير
- `fa-brain` - التحليلات الذكية

---

### 9. **الإعدادات** ⚙️
```
الإعدادات
└── الإعدادات العامة (settings.html)
```

**الأيقونات:**
- `fa-cog` - الإعدادات العامة

---

## 👤 معلومات المشرف (Footer)

```
┌─────────────────────┐
│  👤                 │
│  اسم المشرف         │
│  Super Admin        │
│                  🚪 │
└─────────────────────┘
```

**المحتوى:**
- أيقونة المستخدم: `fa-user-shield`
- اسم المشرف: يُجلب من `localStorage.getItem('admin_username')`
- الدور: "Super Admin"
- زر تسجيل الخروج: `fa-sign-out-alt`

---

## 🎨 المميزات

### 1. **التحديث التلقائي للصفحة النشطة**

```javascript
function setActivePage() {
    const currentPage = window.location.pathname.split('/').pop();
    // يضيف class "active" للصفحة الحالية
}
```

**الفائدة:**
- الصفحة الحالية تظهر بلون مميز
- تحديث تلقائي عند التنقل

### 2. **عدادات الطلبات الديناميكية**

```javascript
async function updatePendingRequestsCount() {
    // يجلب العدد من API كل 30 ثانية
    // يحدث الـ badges تلقائياً
}
```

**الفائدة:**
- عرض عدد الطلبات المعلقة في الوقت الفعلي
- تحديث تلقائي كل 30 ثانية
- يختفي الـ badge إذا كان العدد = 0

### 3. **الطي والتوسيع**

```javascript
sidebarToggle.addEventListener('click', () => {
    sidebar.classList.toggle('collapsed');
    localStorage.setItem('sidebar-collapsed', ...);
});
```

**الفائدة:**
- إمكانية طي القائمة لمساحة أكبر
- حفظ الحالة في localStorage
- استرجاع الحالة عند إعادة التحميل

### 4. **التصميم المتجاوب (Responsive)**

```javascript
// إغلاق تلقائي على الموبايل عند الضغط خارج القائمة
document.addEventListener('click', (e) => {
    if (window.innerWidth <= 768) {
        if (!sidebar.contains(e.target)) {
            sidebar.classList.remove('active');
        }
    }
});
```

**الفائدة:**
- تصميم مناسب للموبايل
- قائمة منبثقة على الشاشات الصغيرة
- إغلاق تلقائي عند الضغط خارجها

---

## 🔧 الاستخدام

### 1. **إضافة القائمة لصفحة جديدة:**

```html
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <!-- ... -->
    <link rel="stylesheet" href="css/unified-sidebar.css">
</head>
<body>
    <!-- Sidebar Container -->
    <div id="sidebarContainer"></div>
    
    <!-- Main Content -->
    <main class="main-content">
        <!-- محتوى الصفحة -->
    </main>
    
    <!-- Scripts -->
    <script src="js/sidebar.js"></script>
</body>
</html>
```

### 2. **تحديث معلومات المشرف:**

```javascript
// يتم تلقائياً من localStorage
localStorage.setItem('admin_username', 'اسم المشرف');
```

### 3. **إضافة عنصر جديد للقائمة:**

```javascript
// في js/sidebar.js:
<a href="new-page.html" class="nav-item" data-page="new-page">
    <i class="fas fa-icon-name"></i>
    <span>اسم الصفحة</span>
</a>
```

---

## 📱 التصميم المتجاوب

### Desktop (> 768px):
- القائمة ثابتة على اليمين
- عرض كامل للنصوص والأيقونات
- إمكانية الطي للحصول على مساحة أكبر

### Mobile (≤ 768px):
- القائمة مخفية افتراضياً
- تظهر عند الضغط على زر القائمة
- تغطي الشاشة بالكامل
- إغلاق تلقائي عند الضغط خارجها

---

## 🎨 الألوان والأيقونات

### الألوان:
- **Primary:** `#059669` (أخضر الدلما)
- **Active:** `#047857` (أخضر داكن)
- **Background:** `#1a1f2e` (Dark Mode)
- **Text:** `#e5e7eb` (فاتح)

### الأيقونات (Font Awesome 6.4.0):
- Dashboard: `fa-chart-line`
- Security: `fa-shield-virus`, `fa-network-wired`, `fa-user-shield`
- Users: `fa-users`
- Requests: `fa-file-alt`
- Realty: `fa-building`, `fa-arrow-up`, `fa-chart-line`
- Finance: `fa-dollar-sign`
- Content: `fa-ad`, `fa-bullhorn`, `fa-bell`
- Analytics: `fa-chart-bar`, `fa-brain`
- Settings: `fa-cog`

---

## 🔄 التحديثات التلقائية

### 1. **عدادات الطلبات:**
```javascript
// تحديث كل 30 ثانية
setInterval(updatePendingRequestsCount, 30000);
```

### 2. **الصفحة النشطة:**
```javascript
// تحديث عند تحميل الصفحة
setActivePage();
```

### 3. **معلومات المشرف:**
```javascript
// تحديث من localStorage
updateAdminInfo();
```

---

## ✅ الخلاصة

**القائمة الجانبية الموحدة توفر:**

✅ **هيكل ثابت** في جميع الصفحات
✅ **تحديث تلقائي** للصفحة النشطة
✅ **عدادات ديناميكية** للطلبات المعلقة
✅ **تصميم متجاوب** (Desktop & Mobile)
✅ **إمكانية الطي** لمساحة أكبر
✅ **حفظ الحالة** في localStorage
✅ **معلومات المشرف** في الأسفل
✅ **تسجيل خروج** سريع

**القائمة جاهزة ومتكاملة! 🎉**

