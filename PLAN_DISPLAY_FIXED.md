# ✅ إصلاح: عرض الباقة الصحيحة

## 🔍 **المشكلة:**

```javascript
❌ API يرجع:
{
  office: {
    plan: {
      code: 'pro',
      name: 'احترافي'
    }
  }
}

❌ لكن refresh-data.js كان يبحث عن:
data.office.plan_code  // ❌ undefined
data.office.plan_name  // ❌ undefined

✅ النتيجة: يستخدم القيم الافتراضية 'free' و 'مجاني'
```

---

## ✅ **الحل:**

### تم تعديل `refresh-data.js`:

```javascript
// قبل:
plan: {
    code: data.office.plan_code || 'free',  // ❌
    name_ar: data.office.plan_name || 'مجاني'  // ❌
}

// بعد:
plan: {
    code: data.office.plan?.code || 'free',  // ✅
    name_ar: data.office.plan?.name || 'مجاني'  // ✅
}
```

---

## 🧪 **اختبر الآن:**

```
1. أعد تحميل الصفحة (Ctrl+Shift+R)
2. افتح Console (F12)
3. ابحث عن:
   ✅ [REFRESH] تم تحديث بيانات المكتب: {code: 'pro', name_ar: 'احترافي', ...}
4. اضغط على "إداري"
5. ✅ يظهر "احترافي" بدلاً من "مجاني"
```

---

## 📊 **التحقق:**

```javascript
// في Console:
JSON.parse(localStorage.getItem('office_data')).plan
// النتيجة المتوقعة:
// {code: 'pro', name_ar: 'احترافي', ...}
```

---

## 🎯 **النتيجة:**

| العنصر | قبل | بعد |
|--------|-----|-----|
| **الباقة** | ❌ مجاني | ✅ احترافي |
| **الكود** | ❌ free | ✅ pro |
| **المصدر** | ❌ افتراضي | ✅ من API |

---

**أعد تحميل الصفحة وجرب!** 🔄✨

